@model IEnumerable<Capital.Domain.SalesIncentive>

<div class="table-responsive scroll3">
    <table class="table table-bordered" id="tab_logic">
@if (Model.Count() > 0)
{
        <thead>
            <tr class="grid_heading">


                <th width="5%" class="span">
                    <table class="table table-bordered table-hover" id="tab_logic">
                        <thead>
                            <tr><th><span class="code">Code</span></th></tr>

                        </thead>
                        <tbody>
                           
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@item.SalesMgCode</td>
                                    </tr>
                            }
                        </tbody>
                    </table>
                </th>

                <th width="15%" class="span">
                    <table class="table table-bordered table-hover" id="tab_logic">
                        <thead>
                            <tr><th><span class="manager">Employee Name</span></th></tr>

                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.SalesMgName</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </th>

                <th width="15%" class="span">
                    <table class="table table-bordered table-hover" id="tab_logic">
                        <thead>
                            <tr><th><span class="code">BenchMark (3*Monthly Salary)</span></th></tr>

                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Benchmark</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </th>

                <th width="15%" class="span">
                    <table class="table table-bordered table-hover" id="tab_logic">
                        <thead>
                            <tr><th><span class="code">incentive%</span></th></tr>

                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.IncentivePerc</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </th>


                <th width="20%" class="span">
                    <table class="table table-bordered table-hover" id="tab_logic">
                        <thead>
                            <tr><th colspan="4"><center>January</center></th></tr>
                            <tr><th width="25%">Tot Commi</th><th width="25%">Incentive Amt</th></tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>

                                    <td width="25%">@item.JanComm</td>
                                    <td width="25%">@item.JanInctve</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </th>

                <th width="20%" class="span">
                    <table class="table table-bordered table-hover" id="tab_logic">
                        <thead>
                            <tr><th colspan="4"><center>February</center></th></tr>
                            <tr><th width="25%">Tot Commi</th><th width="25%">Incentive Amt</th></tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>

                                    <td width="25%">@item.FebComm</td>
                                    <td width="25%">@item.FebInctve</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </th>

                <th width="20%" class="span">
                    <table class="table table-bordered table-hover" id="tab_logic">
                        <thead>
                            <tr><th colspan="4"><center>March</center></th></tr>
                            <tr><th width="25%">Tot Commi</th><th width="25%">Incentive Amt</th></tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>

                                    <td width="25%">@item.MarComm</td>
                                    <td width="25%">@item.MarInctve</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </th>

                <th width="20%" class="span">
                    <table class="table table-bordered table-hover" id="tab_logic">
                        <thead>
                            <tr><th colspan="4"><center>April</center></th></tr>
                            <tr><th width="25%">Tot Commi</th><th width="25%">Incentive Amt</th></tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>

                                    <td width="25%">@item.AplComm</td>
                                    <td width="25%">@item.AplInctve</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </th>

                <th width="20%" class="span">
                    <table class="table table-bordered table-hover" id="tab_logic">
                        <thead>
                            <tr><th colspan="4"><center>May</center></th></tr>
                            <tr><th width="25%">Tot Commi</th><th width="25%">Incentive Amt</th></tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>

                                    <td width="25%">@item.MayComm</td>
                                    <td width="25%">@item.MayInctve</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </th>

                <th width="20%" class="span">
                    <table class="table table-bordered table-hover" id="tab_logic">
                        <thead>
                            <tr><th colspan="4"><center>June</center></th></tr>
                            <tr><th width="25%">Tot Commi</th><th width="25%">Incentive Amt</th></tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>

                                    <td width="25%">@item.JunComm</td>
                                    <td width="25%">@item.JunInctve</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </th>

                <th width="20%" class="span">
                    <table class="table table-bordered table-hover" id="tab_logic">
                        <thead>
                            <tr><th colspan="4"><center>July</center></th></tr>
                            <tr><th width="25%">Tot Commi</th><th width="25%">Incentive Amt</th></tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>

                                    <td width="25%">@item.JulyComm</td>
                                    <td width="25%">@item.JulyInctve</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </th>

                <th width="20%" class="span">
                    <table class="table table-bordered table-hover" id="tab_logic">
                        <thead>
                            <tr><th colspan="4"><center>August</center></th></tr>
                            <tr><th width="25%">Tot Commi</th><th width="25%">Incentive Amt</th></tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>

                                    <td width="25%">@item.AugComm</td>
                                    <td width="25%">@item.AugInctve</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </th>

                <th width="20%" class="span">
                    <table class="table table-bordered table-hover" id="tab_logic">
                        <thead>
                            <tr><th colspan="4"><center>September</center></th></tr>
                            <tr><th width="25%">Tot Commi</th><th width="25%">Incentive Amt</th></tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>

                                    <td width="25%">@item.SepComm</td>
                                    <td width="25%">@item.SepInctve</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </th>

                <th width="20%" class="span">
                    <table class="table table-bordered table-hover" id="tab_logic">
                        <thead>
                            <tr><th colspan="4"><center>October</center></th></tr>
                            <tr><th width="25%">Tot Commi</th><th width="25%">Incentive Amt</th></tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>

                                    <td width="25%">@item.OctComm</td>
                                    <td width="25%">@item.OctInctve</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </th>

                <th width="20%" class="span">
                    <table class="table table-bordered table-hover" id="tab_logic">
                        <thead>
                            <tr><th colspan="4"><center>November</center></th></tr>
                            <tr><th width="25%">Tot Commi</th><th width="25%">Incentive Amt</th></tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>

                                    <td width="25%">@item.NovComm</td>
                                    <td width="25%">@item.NovInctve</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </th>

                <th width="20%" class="span">
                    <table class="table table-bordered table-hover" id="tab_logic">
                        <thead>
                            <tr><th colspan="4"><center>December</center></th></tr>
                            <tr><th width="25%">Tot Commi</th><th width="25%">Incentive Amt</th></tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>

                                    <td width="25%">@item.DecComm</td>
                                    <td width="25%">@item.DecInctve</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </th>



        </thead>
                          
                            }
                            else
                            {
<tr>
    <td colspan="6" align="center" valign="middle">
        <em>pls enter sales target and then select an year</em>
    </td>
</tr>
                            }
    </table>
</div>